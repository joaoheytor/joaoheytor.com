baseURL = 'http://hugo.joaoheytor.com/'
title = 'João Heytor'
theme = 'zen'
languageCode = 'pt-br'

[outputFormats]
  [outputFormats.SearchIndex]
    mediaType = "application/json"
    baseName = "searchindex"
    isPlainText = true
    notAlternative = true

[outputs]
  home = ["HTML","RSS", "JSON", "SearchIndex"]
  section = ["HTML","RSS", "JSON"]

[params]
  cookieConsent =  true
  contact = "jhkpereira@gmail.com"
  copyright = "shubs company"
  dateformat = ""            # Set the date format, default to "2 January, 2006"
  description = ""           # Set site description, used in meta tags and JSON-LD
  feedlinks = true           # Show feed links in the footer.
  footer = "A [example.org](https://example.org/) production."
  jquery = true
  mobileMenu = true
  searchLimit = 30           # Max number of search hits, default 20.
  submitted = true           # Show author and date information for a post.
  imageMaxWidth = 400
  mainSections = "posts"            # The sections you want to have listed on the front page.
  relatedposts = true
  logo = false

#[languages]
#  [languages.pt]
#    weight = 1
#    languageName = "Portuguese"
#    languageCode = "pt-br"
#  [languages.en]
#    weight = 2
#    languageName = "English"
#    languageCode = "en-US"
